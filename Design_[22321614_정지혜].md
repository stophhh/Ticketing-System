# 3. Design

## Project Title

QuickTicket - 티켓 예매 시스템

## Student Information

* Student No: 22321614
* Name: 정지혜
* E-mail: jihye8564@naver.com

---

## Revision History

| Revision date | Version | Description                | Author |
| ------------- | ------: | -------------------------- | ------ |
| 05/31/2026    |    1.00 | Initial design document 작성 | 정지혜    |
| 06/03/2026    |    2.00 | Sequence Diagram, State Machine diagram 추가 | 정지혜 |
---

## Contents

1. Introduction
2. Class Diagram
3. Sequence Diagram
4. State Machine Diagram
5. Implementation Requirements
6. Glossary
7. References

---

# 1. Introduction

본 문서는 QuickTicket 티켓 예매 시스템의 설계 내용을 정리한 문서이다. QuickTicket은 사용자가 공연 목록을 조회하고, 원하는 공연의 상세 정보를 확인한 뒤 대기열을 거쳐 좌석을 선택하고 결제하여 예매를 완료할 수 있는 웹 기반 티켓팅 시스템이다.

본 시스템은 관리자 기능과 사용자 기능으로 나누어 설계하였다. 관리자는 공연을 등록하고 공연별 좌석을 생성할 수 있으며, 사용자는 회원가입 및 로그인 후 공연 조회, 대기열 입장, 좌석 선택, 결제, 예매 내역 조회, 예매 취소를 수행할 수 있다.

설계의 핵심은 공연, 좌석, 예매, 결제, 대기열 객체 간의 관계를 명확히 분리하는 것이다. Performance는 여러 Seat와 Booking을 가질 수 있고, User는 여러 Booking을 가질 수 있다. Booking은 하나 이상의 Seat와 연결되며, Payment는 Booking과 1:1 관계를 가진다. 이를 통해 실제 티켓팅 서비스와 유사한 예매 흐름을 구현할 수 있도록 설계하였다.

---

# 2. Class Diagram

## 2.1 Class Diagram Overview

본 시스템의 클래스 다이어그램은 Controller, Service, Repository, Entity 계층으로 구성된다.

* Controller 계층은 사용자 요청을 받고 Service 계층에 처리를 위임한다.
* Service 계층은 비즈니스 로직을 수행한다.
* Repository 계층은 데이터베이스 접근을 담당한다.
* Entity 계층은 시스템의 핵심 데이터를 표현한다.

주요 클래스는 다음과 같다.

---

## 2.2 Admin Related Classes

| Class                 | Type       | Description                                  |
| --------------------- | ---------- | -------------------------------------------- |
| AdminController       | Controller | 관리자 기능을 처리한다. 공연 등록, 공연 삭제, 공연별 좌석 생성을 담당한다. |
| PerformanceService    | Service    | 공연 조회, 공연 등록, 공연 삭제 기능을 담당한다.                |
| SeatService           | Service    | 좌석 선점, 좌석 자동 해제, 공연별 좌석 생성 기능을 담당한다.         |
| PerformanceRepository | Repository | Performance 엔티티의 DB 접근을 담당한다.                |
| SeatRepository        | Repository | Seat 엔티티의 DB 접근을 담당한다.                       |
| Performance           | Entity     | 공연 정보를 표현한다.                                 |
| Seat                  | Entity     | 좌석 정보를 표현한다.                                 |

### AdminController

| Attribute / Method                      | Description            |
| --------------------------------------- | ---------------------- |
| performanceService : PerformanceService | 공연 관련 비즈니스 로직을 호출한다.   |
| seatService : SeatService               | 좌석 관련 비즈니스 로직을 호출한다.   |
| createPerformance()                     | 관리자가 공연을 등록한다.         |
| deletePerformance()                     | 관리자가 공연을 삭제한다.         |
| createSeats()                           | 특정 공연에 대한 좌석을 자동 생성한다. |

### Performance

| Attribute                       | Description |
| ------------------------------- | ----------- |
| performanceId : Long            | 공연 고유 ID    |
| title : String                  | 공연명         |
| place : String                  | 공연 장소       |
| performanceDate : LocalDateTime | 공연 일시       |
| price : int                     | 기본 가격       |
| description : String            | 공연 설명       |
| posterUrl : String              | 포스터 이미지 경로  |
| category : String               | 공연 카테고리     |

### Seat

| Attribute                     | Description |
| ----------------------------- | ----------- |
| id : Long                     | 좌석 고유 ID    |
| seatNumber : String           | 좌석 번호       |
| grade : String                | 좌석 등급       |
| price : int                   | 좌석 가격       |
| status : SeatStatus           | 좌석 상태       |
| holdExpiresAt : LocalDateTime | 좌석 선점 만료 시간 |
| performance : Performance     | 좌석이 속한 공연   |

### Main Relationships

| Relationship                               | Meaning                 |
| ------------------------------------------ | ----------------------- |
| Performance 1 — 0..* Seat                  | 하나의 공연은 여러 좌석을 가진다.     |
| AdminController — PerformanceService       | 관리자 컨트롤러는 공연 서비스를 사용한다. |
| AdminController — SeatService              | 관리자 컨트롤러는 좌석 서비스를 사용한다. |
| PerformanceService — PerformanceRepository | 공연 서비스는 공연 저장소를 사용한다.   |
| SeatService — SeatRepository               | 좌석 서비스는 좌석 저장소를 사용한다.   |

---

## 2.3 User, Booking, Payment, Queue Related Classes

| Class             | Type       | Description                        |
| ----------------- | ---------- | ---------------------------------- |
| UserController    | Controller | 회원가입, 로그인, 로그아웃 기능을 처리한다.          |
| UserService       | Service    | 사용자 계정 관련 비즈니스 로직을 처리한다.           |
| BookingController | Controller | 예매 생성, 예매 조회, 예매 취소 요청을 처리한다.      |
| BookingService    | Service    | 예매 생성, 예매 취소, 사용자별 예매 조회 로직을 처리한다. |
| PaymentController | Controller | 결제 요청, 결제 확정, 결제 취소 요청을 처리한다.      |
| PaymentService    | Service    | 결제 상태 변경과 예매 상태 변경을 처리한다.          |
| QueueController   | Controller | 대기열 입장 및 대기 상태 확인 요청을 처리한다.        |
| QueueService      | Service    | 대기열 생성, 확인, 입장 허용 로직을 처리한다.        |
| QueueScheduler    | Scheduler  | 대기열 상태를 주기적으로 갱신한다.                |
| SeatScheduler     | Scheduler  | 선점 시간이 만료된 좌석을 자동 해제한다.            |

### User

| Attribute           | Description |
| ------------------- | ----------- |
| id : Long           | 사용자 고유 ID   |
| username : String   | 로그인 아이디     |
| password : String   | 암호화된 비밀번호   |
| ip : String         | 가입 IP       |
| userType : UserType | 사용자 유형      |

### Booking

| Attribute                   | Description |
| --------------------------- | ----------- |
| bookingId : Long            | 예매 고유 ID    |
| bookingTime : LocalDateTime | 예매 시간       |
| status : BookingStatus      | 예매 상태       |
| user : User                 | 예매한 사용자     |
| seats : List<Seat>          | 예매한 좌석 목록   |
| performance : Performance   | 예매한 공연      |

### Payment

| Attribute              | Description |
| ---------------------- | ----------- |
| id : Long              | 결제 고유 ID    |
| amount : int           | 결제 금액       |
| method : String        | 결제 수단       |
| status : PaymentStatus | 결제 상태       |
| paidAt : LocalDateTime | 결제 완료 시간    |
| booking : Booking      | 결제 대상 예매    |

### QueueEntry

| Attribute                 | Description |
| ------------------------- | ----------- |
| id : Long                 | 대기열 고유 ID   |
| queueNumber : int         | 대기 번호       |
| status : QueueStatus      | 대기 상태       |
| joinedAt : LocalDateTime  | 대기열 입장 시간   |
| expiresAt : LocalDateTime | 입장 가능 만료 시간 |

### Main Relationships

| Relationship                   | Meaning                   |
| ------------------------------ | ------------------------- |
| User 1 — 0..* Booking          | 한 사용자는 여러 예매를 가질 수 있다.    |
| Performance 1 — 0..* Booking   | 하나의 공연은 여러 예매를 가질 수 있다.   |
| Booking 1 — 1..* Seat          | 하나의 예매는 하나 이상의 좌석을 포함한다.  |
| Booking 1 — 1 Payment          | 하나의 예매는 하나의 결제와 연결된다.     |
| Performance 1 — 0..* Seat      | 하나의 공연은 여러 좌석을 가진다.       |
| QueueService — QueueRepository | 대기열 서비스는 대기열 저장소를 사용한다.   |
| SeatScheduler — SeatService    | 좌석 스케줄러는 만료된 좌석 선점을 해제한다. |

---

# 3. Sequence Diagram

## 3.1 User Login Sequence

### Participants

* User
* UserController
* UserService
* UserRepository

### Description

로그인 시퀀스 다이어그램은 사용자가 로그인 정보를 입력했을 때 인증이 처리되는 과정을 표현한다.

사용자가 `login(username, password)` 요청을 보내면 `UserController`는 `UserService`에 로그인 처리를 요청한다. `UserService`는 `UserRepository`를 통해 입력된 아이디에 해당하는 사용자를 조회한다.

사용자가 존재하지 않으면 로그인 실패 결과를 반환한다. 사용자가 존재하는 경우에는 입력한 비밀번호와 저장된 비밀번호가 일치하는지 확인한다. 비밀번호가 일치하지 않으면 로그인 실패 결과를 반환하고, 일치하면 로그인 성공 결과를 반환한다.

이 다이어그램에서는 사용자 존재 여부와 비밀번호 일치 여부를 각각 `alt` 프레임으로 표현하였다.

---

## 3.2 signup Sequence

### Participants

* User
* UserController
* UserService
* UserRepository

### Description

회원가입 시퀀스 다이어그램은 사용자가 회원가입 요청을 보냈을 때, `UserController`, `UserService`, `UserRepository`가 어떤 순서로 동작하는지를 나타낸다.

사용자가 `signup(username, password)` 요청을 보내면 `UserController`는 이를 받아 `UserService`의 회원가입 기능을 호출한다. `UserService`는 먼저 `UserRepository`를 통해 입력된 아이디가 이미 존재하는지 확인한다.

아이디가 이미 존재하는 경우에는 회원가입 실패 결과를 반환한다. 반대로 아이디가 존재하지 않는 경우에는 사용자 정보를 저장하고 회원가입 성공 결과를 반환한다. 이 과정에서 `alt` 프레임을 사용하여 아이디 중복 여부에 따른 흐름을 구분하였다.


---

## 3.3 SeatSelect Sequence

### Participants

* User
* SeatController
* SeatService
* SeatRepository

### Description

좌석 선택 시퀀스 다이어그램은 사용자가 공연의 좌석 목록을 조회하고, 특정 좌석을 선택하여 임시 선점하는 과정을 나타낸다.

먼저 사용자는 공연 ID를 기준으로 좌석 목록 조회를 요청한다. `SeatController`는 이를 `SeatService`에 전달하고, `SeatService`는 `SeatRepository`를 통해 해당 공연의 좌석 목록을 조회한 뒤 사용자에게 반환한다.

이후 사용자가 특정 좌석을 선택하면 `holdSeat(seatId)` 요청이 전달된다. `SeatService`는 `SeatRepository`의 `findByIdForUpdate(seatId)`를 사용하여 좌석을 조회하고, 좌석 상태를 확인한다.

좌석 상태가 `RESERVED`이면 이미 예약된 좌석이므로 실패를 반환하고, `HELD`이면 이미 다른 사용자가 선택한 좌석이므로 실패를 반환한다. 좌석 상태가 `AVAILABLE`이면 좌석 상태를 `HELD`로 변경하고, 선점 만료 시간을 설정한 뒤 좌석 선택 성공 결과를 반환한다.

---

## 3.4 Create Booking Sequence

### Participants

* User
* BookingController
* BookingService
* UserRepository
* PerformanceRepository
* SeatRepository
* BookingRepository

### Description

예매 생성 시퀀스 다이어그램은 사용자가 선택한 좌석을 실제 예매 정보로 생성하는 과정을 나타낸다.

사용자가 `createBooking(request)` 요청을 보내면 `BookingController`는 요청 정보에서 사용자 ID, 공연 ID, 좌석 ID 목록을 추출하여 `BookingService`에 전달한다.

`BookingService`는 사용자, 공연, 좌석 정보를 조회하고, 선택된 좌석들이 예매 가능한 상태인지 확인한다. 이때 선택된 좌석 중 하나라도 `HELD` 상태가 아니라면 예매 실패 결과를 반환한다.

모든 선택 좌석이 `HELD` 상태이면 각 좌석의 상태를 `RESERVED`로 변경하고, 선점 만료 시간을 제거한다. 이후 예매 객체를 생성하고 `BookingRepository`를 통해 저장한 뒤 예매 성공 결과를 반환한다.

이 다이어그램에서는 여러 좌석을 처리하는 부분을 `loop` 프레임으로 표현하였다.


---

## 3.5 Cancel Booking Sequence

### Participants

* User
* BookingController
* BookingService
* BookingRepository


### Description

예매 취소 시퀀스 다이어그램은 사용자가 기존 예매를 취소했을 때 예매 상태와 좌석 상태가 변경되는 과정을 나타낸다.

사용자가 `cancelBooking(bookingId)` 요청을 보내면 `BookingController`는 이를 `BookingService`에 전달한다. `BookingService`는 내부적으로 `getBooking(bookingId)`를 호출하여 예매 정보를 조회하고, `BookingRepository`를 통해 해당 예매가 존재하는지 확인한다.

예매 정보가 존재하지 않으면 예매 취소 실패 결과를 반환한다. 예매 정보가 존재하면 예매 상태를 `CANCELED`로 변경한다. 이후 예매에 포함된 좌석들을 반복하면서 각 좌석 상태를 `AVAILABLE`로 변경하고, 선점 만료 시간을 `null`로 초기화한다.

마지막으로 변경된 예매 정보를 저장하고 예매 취소 완료 결과를 반환한다.

---

## 3.6 Payment Sequence

### Participants

* User
* PaymentController
* PaymentService
* BookingRepository
* PaymentRepository

### Description

결제 시퀀스 다이어그램은 결제 요청과 결제 승인 과정을 표현한다.

먼저 사용자는 `requestPayment(request)`를 통해 결제 요청을 보낸다. `PaymentController`는 이를 `PaymentService`에 전달하고, `PaymentService`는 `BookingRepository`를 통해 결제 대상 예매 정보를 조회한다. 예매 정보가 확인되면 결제 객체를 생성하고 `PaymentRepository`에 저장한 뒤 결제 요청 완료 결과를 반환한다.

이후 사용자가 `confirmPayment(paymentId)`를 요청하면 `PaymentController`는 `PaymentService`에 결제 승인 처리를 요청한다. `PaymentService`는 `PaymentRepository`에서 결제 정보를 조회한다.

결제 정보가 존재하지 않으면 결제 실패 결과를 반환한다. 결제 정보가 존재하면 결제 상태를 `COMPLETED`로 변경하고 결제 완료 시간을 설정한다. 이후 변경된 결제 정보를 저장하고 결제 성공 결과를 반환한다.

결제 정보 존재 여부에 따른 흐름은 `alt` 프레임을 사용하여 표현하였다.


---

## 3.7 Admin Sequence

### Participants

* Admin
* AdminController
* AdminService
* PerformanceRepository
* SeatService

### Description

관리자 공연관리 시퀀스 다이어그램은 관리자가 공연 정보를 등록, 수정, 삭제하는 과정을 나타낸다.

공연 등록 시 관리자는 `createPerformance(request)` 요청을 보내고, `AdminController`는 이를 `AdminService`에 전달한다. `AdminService`는 공연 객체를 생성한 뒤 `PerformanceRepository`를 통해 저장한다. 필요한 경우 `SeatService`를 호출하여 공연에 해당하는 좌석을 함께 생성한다. 좌석 생성 여부는 `opt` 프레임으로 표현하였다.

공연 수정 시 관리자는 `updatePerformance(performanceId, request)` 요청을 보낸다. `AdminService`는 `PerformanceRepository`를 통해 수정 대상 공연을 조회한다. 공연이 존재하지 않으면 수정 실패 결과를 반환한다. 공연이 존재하면 제목, 장소, 날짜, 가격 등의 정보를 수정하고, 수정된 공연 정보를 다시 저장한 뒤 수정 성공 결과를 반환한다. 공연 존재 여부에 따른 흐름은 `alt` 프레임으로 표현하였다.

공연 삭제 시 관리자는 `deletePerformance(performanceId)` 요청을 보낸다. `AdminService`는 `PerformanceRepository`를 통해 해당 공연 정보를 삭제하고 삭제 성공 결과를 반환한다.

관리자 기능은 일반 사용자 기능과 달리 공연 데이터를 직접 관리하는 흐름이므로, 공연 엔티티와 저장소의 동작을 중심으로 표현하였다.



---

# 4. State Machine Diagram


---

# 5. Implementation Requirements

## 5.1 Development Environment

| Item            | Description                 |
| --------------- | --------------------------- |
| Language        | Java                        |
| Framework       | Spring Boot                 |
| IDE             | IntelliJ IDEA               |
| Build Tool      | Gradle                      |
| Database        | MySQL                       |
| ORM             | Spring Data JPA / Hibernate |
| Frontend        | HTML, CSS, JavaScript       |
| Version Control | Git, GitHub                 |

## 5.2 Runtime Environment

| Item              | Description               |
| ----------------- | ------------------------- |
| JDK               | Java 17                   |
| DBMS              | MySQL                     |
| Server Port       | 8080                      |
| Execution Command | `./gradlew bootRun`       |
| Build Command     | `./gradlew clean bootJar` |

## 5.3 Main Implementation Requirements

* 사용자는 회원가입 및 로그인을 할 수 있어야 한다.
* 관리자는 공연을 등록, 삭제할 수 있어야 한다.
* 관리자는 공연별 좌석을 자동 생성할 수 있어야 한다.
* 사용자는 공연 목록과 공연 상세 정보를 조회할 수 있어야 한다.
* 사용자는 예매 전 대기열에 입장해야 한다.
* 사용자는 최대 4매까지 좌석을 선택할 수 있어야 한다.
* 선택된 좌석은 일정 시간 동안 HELD 상태가 되어야 한다.
* 결제가 완료되면 Booking은 COMPLETED 상태가 되고 Seat는 RESERVED 상태가 되어야 한다.
* 사용자는 마이페이지에서 예매 내역을 확인할 수 있어야 한다.
* 사용자는 예매를 취소할 수 있어야 한다.
* 예매 취소 시 좌석은 다시 AVAILABLE 상태로 변경되어야 한다.

---

# 6. Glossary

| Term        | Description                 |
| ----------- | --------------------------- |
| Performance | 예매 가능한 공연 정보                |
| Seat        | 공연에 속한 좌석                   |
| Booking     | 사용자가 선택한 공연과 좌석에 대한 예매 정보   |
| Payment     | 예매에 대한 결제 정보                |
| Queue       | 사용자가 예매 페이지에 입장하기 전 대기하는 구조 |
| QueueEntry  | 대기열에 등록된 사용자 요청 정보          |
| HELD        | 좌석이 임시 선점된 상태               |
| RESERVED    | 좌석이 결제 완료 후 예약 확정된 상태       |
| AVAILABLE   | 좌석 선택이 가능한 상태               |
| PENDING     | 예매 또는 결제가 아직 완료되지 않은 상태     |
| COMPLETED   | 예매 또는 결제가 완료된 상태            |
| CANCELED    | 예매 또는 결제가 취소된 상태            |
| Repository  | 데이터베이스 접근 계층                |
| Service     | 비즈니스 로직 처리 계층               |
| Controller  | HTTP 요청 처리 계층               |
| DTO         | 계층 간 데이터 전달 객체              |

---

# 7. References

* Spring Boot Documentation
* Spring Data JPA Documentation
* Hibernate ORM Documentation
* MySQL Documentation
* StarUML User Guide
* HTML / CSS / JavaScript MDN Web Docs
* Course materials for Object-Oriented Analysis and Design
